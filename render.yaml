services:
  - type: web
    name: bizcard-bot
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: MAKE_WEBHOOK_URL
        sync: false
